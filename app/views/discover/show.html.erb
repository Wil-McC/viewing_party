<h1>Welcome <%= current_user.email %></h1>

<%= link_to 'Top Rated', movies_path(query: 'top_forty') %>

<%= form_with url: movies_path, method: :get, local: true do |f| %>
  <%= f.label :query, "Search Movies:" %>
  <%= f.text_field :query %>
  <%= f.submit 'Search' %>
<% end %>
